{% macro senseList(senses, currentSense, tail) %}
    <div class="sense-list">
        {% for sense in senses %}
            <div class="sense">
                {% if currentSense and sense.is(currentSense) %}
                    <strong>{{ sense.name }}</strong>
                {% else %}
                    <a href="{{ path('search', { searchString: sense.name }) }}">{{ sense.name }}</a>
                {%- endif %}
                {{- loop.last ? '' : ',' }}
            </div>
        {% endfor %}
        {{ tail }}
    </div>
{% endmacro %}
