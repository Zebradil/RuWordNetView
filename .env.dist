CERTBOT_EMAIL=
POSTGRES_PASSWORD=
POSTGRES_USER=
POSTGRES_DB=
# POSTGRES_HOST and POSTGRES_PORT are set automatically by compose.yaml
# (host=database, port=5432). Override here only if running outside compose.
